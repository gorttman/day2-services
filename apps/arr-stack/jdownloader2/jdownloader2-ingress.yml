apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jdownloader2
  namespace: arr-stack
  labels:
    app: arr-stack
    app.kubernetes.io/name: jdownloader2
    argocd.argoproj.io/managed-by: argocd
  annotations:
    cert-manager.io/cluster-issuer: "private-ca"
spec:
  # Internal-only: no Cloudflare Tunnel annotation; DNS in dns-conf.
  # Backend is the shared arr-stack pod, i.e. traffic enters through
  # Gluetun's network namespace and firewall (FIREWALL_INPUT_PORTS).
  ingressClassName: traefik
  tls:
  - hosts:
    - jdownloader.i3sec.com.au
    secretName: jdownloader2-tls
  rules:
  - host: jdownloader.i3sec.com.au
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: jdownloader2
            port:
              number: 5800
