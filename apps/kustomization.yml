apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pihole/pihole-app.yml
  - novnc/novnc-app.yml
  - cert-manager/cert-manager-app.yml
  - obsidian/obsidian-app.yml
  - vscode-server/vscode-server-app.yml
  - ca-portal/ca-portal-app.yml
  - postgres/postgres-app.yml
  - paperless/paperless-app.yml
  - jellyfin/jellyfin-app.yml
  - kavita/kavita-app.yml
  # arr-stack disabled 2026-07-17: gluetun CrashLoopBackOff on placeholder
  # VPN secret. Deployment already scaled to 0 (see arr-stack-deployment.yml)
  # so the orphaned Application resources stay stopped, not crash-looping,
  # once Argo CD prunes the Application object below. Re-add this line and
  # flip replicas back to 1 once real Mullvad creds are sealed.
  # - arr-stack/arr-stack-app.yml
  - homeassistant/homeassistant-app.yml
  - n8n/n8n-app.yml
