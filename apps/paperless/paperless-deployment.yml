apiVersion: apps/v1
kind: Deployment
metadata:
  name: paperless
  namespace: paperless
  labels:
    app: paperless
    app.kubernetes.io/name: paperless
    app.kubernetes.io/component: webserver
    app.kubernetes.io/part-of: day2-services
    argocd.argoproj.io/managed-by: argocd
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: paperless
  template:
    metadata:
      labels:
        app: paperless
        app.kubernetes.io/name: paperless
    spec:
      # pinode-01's k3s root is RAM-backed tmpfs (see
      # day2-services/apps/books-pipeline for the same fix) - this image
      # (OCR + scipy/numpy deps) is too large to unpack there: "no space
      # left on device" on 2026-08-01 confirmed this isn't hypothetical.
      nodeSelector:
        kubernetes.io/hostname: k8smaster
      # The 'paperless' Service otherwise injects PAPERLESS_PORT=tcp://...
      # (legacy Docker service links), which Paperless misreads as its own
      # port config and crashes granian on boot
      enableServiceLinks: false
      containers:
      - name: paperless
        image: ghcr.io/paperless-ngx/paperless-ngx:2.20.15
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8000
        env:
        # consume is a raw NFS export written by a different pod
        # (inbox-router) - inotify only fires for writes made through the
        # same client's local mount, so it never sees a second NFS
        # client's changes. Confirmed live 2026-08-01: a test file sat in
        # consume/ unconsumed with the log showing "Using inotify to
        # watch directory" until this was set. 30s matches Paperless's
        # own docs recommendation for network-share consume dirs.
        - name: PAPERLESS_CONSUMER_POLLING
          value: "30"
        - name: PAPERLESS_REDIS
          value: redis://paperless-redis:6379
        - name: PAPERLESS_DBHOST
          value: postgres.postgres.svc.cluster.local
        - name: PAPERLESS_DBNAME
          value: paperless
        - name: PAPERLESS_DBUSER
          value: paperless
        - name: PAPERLESS_DBPASS
          valueFrom:
            secretKeyRef:
              name: paperless-secret
              key: PAPERLESS_DBPASS
        - name: PAPERLESS_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: paperless-secret
              key: PAPERLESS_SECRET_KEY
        - name: PAPERLESS_ADMIN_USER
          value: admin
        - name: PAPERLESS_ADMIN_MAIL
          value: gorttman@i3sec.com.au
        - name: PAPERLESS_ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: paperless-secret
              key: PAPERLESS_ADMIN_PASSWORD
        - name: PAPERLESS_URL
          value: https://paperless.i3sec.com.au
        # Consume folder is now wired (see the consume volume below); no
        # PAPERLESS_CONSUMER_* tuning or OCR language override (default
        # eng) yet - tag/correspondent rules are a later, separate pass.
        volumeMounts:
        - name: data
          mountPath: /usr/src/paperless/data
        - name: media
          mountPath: /usr/src/paperless/media
        - name: consume
          mountPath: /usr/src/paperless/consume
          subPath: consume
        resources:
          requests:
            memory: 512Mi
            cpu: 250m
          limits:
            memory: 2Gi
            cpu: "2"
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 120
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: paperless-data
      - name: media
        persistentVolumeClaim:
          claimName: paperless-media
      # Raw QNAP export, not a PVC - inbox-router writes into this same
      # export (qnap.i3sec.com.au:/paperless) from its own pod, so this has
      # to be a directly-mounted multi-consumer NFS volume like /books and
      # /inbox, not a single-writer PVC. subPath (in the volumeMount below,
      # not here - not a valid NFSVolumeSource field) selects the consume/
      # subdirectory, same pattern arr-stack uses for its books/import
      # handoff. See day1-foundation/apps/qnap-storage/README.md,
      # "paperless directory" section (2026-08-01).
      - name: consume
        nfs:
          server: qnap.i3sec.com.au
          path: /paperless
